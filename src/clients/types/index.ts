export type CardArticle = {
  id: string;
  title: string;
  description: string;
  body: string;
  imageUrl: string;
};

export type CardCategory = {
  id: string;
  title: string;
  description: string;
};

export type CardTag = {
  id: string;
  title: string;
  description: string;
};
